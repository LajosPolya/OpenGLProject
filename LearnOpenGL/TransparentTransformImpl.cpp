#include "TransparentTransformImpl.h"

TransparentTransformImpl::TransparentTransformImpl() {}

TransparentTransformImpl::~TransparentTransformImpl() {}

void TransparentTransformImpl::refreshModel()
{
	// InstancedTransformImpl::getGameObject();

	/*InstancedTransformImpl::getGameObject();
	std::map<float, glm::vec3> sortedWindowPosition;
	for (GLuint i = 0; i < (*(this->getPositions())).size(); i++) {
	GLfloat distance = glm::length(camera.Position - (*(this->getPositions()))[i]);
	sortedWindowPosition[distance] = (*(this->getPositions()))[i];
	}*/
}
